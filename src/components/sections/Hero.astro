---
---
<section class="hero">
  <div class="container">
    <h1>أفضل منصة لتعلم الألمانية بخطوات سهلة وواضحة!</h1>
    <p class="hero-text">
      ابدأ رحلتك الآن من الصفر إلى الطلاقة مع خطة شاملة، أدوات تفاعلية، وشهادات معتمدة تساعدك في تحقيق أهدافك
    </p>
    <div class="hero-features">
      <div class="feature">✓ تعلم بأسلوب ذاتي يناسب وقتك</div>
      <div class="feature">✓ مناهج معتمدة عالمياً</div>
      <div class="feature">✓ تمارين تفاعلية وشهادات معتمدة</div>
    </div>
    <div class="cta-buttons">
      <a href="#start" class="btn btn-primary">ابدأ الآن</a>
      <a href="#demo" class="btn btn-secondary">شاهد عرضاً توضيحياً</a>
    </div>
  </div>
</section>

<style>
.hero {
  padding: 8rem 0 6rem;
  background: linear-gradient(to bottom, var(--bg-warm), var(--white));
  text-align: center;
}

.hero h1 {
  font-size: 3rem;
  margin-bottom: 1.5rem;
  color: var(--text-primary);
}

.hero-text {
  font-size: 1.25rem;
  color: var(--text-secondary);
  max-width: 800px;
  margin: 0 auto 2rem;
}

.hero-features {
  display: flex;
  justify-content: center;
  gap: 2rem;
  margin-bottom: 2rem;
}

.feature {
  color: var(--text-primary);
  font-weight: 500;
}

@media (max-width: 768px) {
  .hero h1 {
    font-size: 2rem;
  }
  
  .hero-features {
    flex-direction: column;
    gap: 1rem;
  }
}
</style>